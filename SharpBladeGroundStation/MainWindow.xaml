<Window x:Class="SharpBladeGroundStation.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SharpBladeGroundStation"
        xmlns:fd="clr-namespace:FlightDisplay;assembly=FlightDisplay"
        mc:Ignorable="d"
        Title="SharpBladeGroundStation" Height="700" Width="1200">
    <Window.Resources>

    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300*" MaxWidth="400" MinWidth="250"/>
            <ColumnDefinition Width="892*"/>
        </Grid.ColumnDefinitions>
        <Grid >
            <Grid.RowDefinitions>
                <RowDefinition Height="300*" MaxHeight="400" MinHeight="250"/>
                <RowDefinition Height="184*"/>
                <RowDefinition Height="185*"/>
            </Grid.RowDefinitions>
            <fd:PFDControl x:Name="pfd" Grid.Row="0" HorizontalAlignment="Left" VerticalAlignment="Top" />

        </Grid>
        <TabControl x:Name="tabControl" Grid.Column="1" Margin="0">
            <TabItem Header="TabItem">
                <Grid Background="#FFE5E5E5"/>
            </TabItem>
            <TabItem Header="TabItem">
                <Grid Background="#FFE5E5E5"/>
            </TabItem>
        </TabControl>


    </Grid>
</Window>
